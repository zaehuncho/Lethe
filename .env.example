# Lethe shard-gate secrets — copy this file to `.env` and fill in.
# NEVER commit the real .env (it is in .gitignore).

# Bearer token for --shard-auth (packer build-time upload)
ORION_BUILDER_SECRET=

# Worker <-> license API shared secret
ORION_EDGE_AUTH_SECRET=
